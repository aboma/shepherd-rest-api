<div class="well">
	<ul class="nav nav-list">
		<li class="nav-header">
			Portfolios
			{{#view Ember.Button action="shownew" targetBinding="controller" 
		 		disabledBinding="controller.shownewform" class="btn"}} 
		 	Create {{/view}}
		</li>
	  	{{#each Luxin.portfoliosAdminController}}
	  		{{#view Luxin.PortfolioView contentBinding="this"}}
    			<li {{bindAttr class="isSelected:active isDeleted:deleted"}} ><a href='#'>{{content.name}}</a></li>
	    	{{/view}}
	 	{{/each}}
	</ul>
</div>