<div class="well">
	<ul class="nav nav-list">
		<li class="nav-header">
			Portfolios
			{{#view Ember.Button action="shownew" targetBinding="controller" 
		 		disabledBinding="controller.shownewform" class="btn"}} 
		 	Create {{/view}}
		</li>
	  	{{#each Luxin.portfoliosController}}
	  		{{#view Luxin.PortfolioView contentBinding="this"}}
	  			{{#unless content.deletedAt  }}
	    			<li {{bindAttr class="isSelected:active"}} ><a href='#'>{{content.name}}</a></li>
	    		{{/unless}}
	    	{{/view}}
	 	{{/each}}
	</ul>
</div>